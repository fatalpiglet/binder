# Исправления UI — Doctor Binder v2.1

## Что было исправлено:

### 1. Цветовая палитра (THEME)
- Фон темнее: `11111b` (для большего контраста)
- Текст светлее: `bac2de` (лучше читаемость)
- Акцент: `74c7ec` (ярче)
- Успех: `90c5a1`, Предупреждение: `fab387`, Ошибка: `f28fad`
- Радиусы скругления: 8/12/16

### 2. Кнопки (03_UI.ahk)
- **NEW**: Add 3rd parameter `isPressed` to `ApplyVisual()`
- Pressed-эффект: текст смещается на 1px при нажатии
- Все вызовы ApplyVisual обновлены

### 3. Цвета кнопок (GetColors)
```
success:  bg 2d4f3e → hover 3d6b52 → pressed 264133
danger:   bg 5c3442 → hover 784456 → pressed 4a2a36
info:     bg 2a4068 → hover 3b5588 → pressed 223558
warning:  bg 5c4a25 → hover 776233 → pressed 4a3b1e
default:  bg 2a2b3d → hover 3b3d52 → pressed 24253a
```

### 4. Исправлена видимость кнопок
- **OLD**: `text: "66687a"` (не видно на тёмном фоне)
- **NEW**: `text: "4a4d66"` (видимый текст для disabled кнопок)
- Функция `UpdateButtonState` исправлена

### 5. Навигация (10_MainGui.ahk)
- Hover-эффект на неактивных вкладках:
  - Фон меняется на `313244`
  - Текст: `bac2de` (светлый)
  - Активные вкладки не меняются при hover

## НЕ изменено (сохранено):
- ✅ Все координаты
- ✅ Размеры карточек и таблиц
- ✅ Отступы и layout
- ✅ Логика работы кнопок
- ✅ Все callback-функции
- ✅ Структура вкладок

## Проверка:
1. Запустите `google.ahk`
2. Проверьте hover на всех кнопках
3. Проверьте нажатие кнопок (pressed)
4. Проверьте переключение вкладок
5. Проверьте disabled-кнопки (должны быть видны)
